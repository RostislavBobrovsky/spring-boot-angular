server:
    port: 8080

spring:
  profiles:
    active: dev
  datasource:
    url: jdbc:postgresql://localhost:5432/spring_boot_test
    username: postgres
    password: root
    driverClassName: org.postgresql.Driver