server:
    port: 8080

spring:
  profiles:
    active: dev
  datasource:
    driverClassName: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/students_spring_boot_angular
    username: postgres
    password: root
    maximumPoolSize: 10